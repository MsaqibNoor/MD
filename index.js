/**
 * @format
 */

import {AppRegistry} from 'react-native';
import MidMain from '.Components/MidMain';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => MidMain);
